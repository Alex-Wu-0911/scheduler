# 项目当前整体流程

本文对应当前源码：Scheduler 负责登记、去重和分发，Worker 负责并发执行。任务执行目前通过 `Thread.sleep(durationMs)` 模拟；执行完成只打印 Worker 日志，尚未回报 Scheduler。

## 1. 整体架构

```text
Client --SUBMIT--> Scheduler :8080
                      |
                  校验 + ID 去重
                      |
                 选择 ALIVE Worker
                      |
                   EXECUTE
                      v
                 Worker :9001
                      |
              3 个执行线程 + 10 个等待位置
                      |
              模拟执行，完成后打印日志

Worker --接收确认--> Scheduler --提交结果--> Client
Worker --独立心跳--> Scheduler --定时检查--> Worker 状态
```

[查看架构图片](docs/diagrams/flow-1.png)

[Mermaid source](docs/diagrams/flow-1.mmd)

Worker 是长期运行的进程，一个 Worker 可以同时执行多个 Task。Scheduler 自身不再执行任务，也没有待执行任务队列；`tasks` 是任务记录表，真正的执行等待队列在 Worker 内部。

## 2. 启动与注册

```text
Scheduler 启动，监听 8080，启动每秒一次的心跳检查
    |
Worker 启动，先绑定 9001
    |
发送 REGISTER|worker-1|localhost|9001
    |
Scheduler 校验参数，并用 putIfAbsent 登记
    +-- 新 ID：返回 REGISTERED，初始状态 ALIVE
    |      |
    |   Worker 启动独立心跳，进入 accept 接收循环
    |
    +-- 已有 ID：返回 ALREADY_REGISTERED，Worker 启动失败
    |
    +-- 参数非法：返回 ERROR，Worker 启动失败
```

[查看注册时序图片](docs/diagrams/flow-2.png)

[Mermaid source](docs/diagrams/flow-2.mmd)

启动顺序为 Scheduler → Worker → Client。当前入口固定使用 `localhost:8080`、Worker ID `worker-1` 和端口 `9001`，没有命令行参数配置。

## 3. Scheduler 接收和分发任务

```text
SUBMIT -> 校验格式、ID、时长
    +-- 不合法 -> ERROR 或 INVALID_TASK
    |
    v
tasks.putIfAbsent
    +-- ID 已存在 -> DUPLICATE
    |
    v
选择 ALIVE Worker
    +-- 没有 -> 移除本次记录，NO_AVAILABLE_WORKER
    |
    v
dispatchTask：发送 EXECUTE，等待接收确认
    +-- ACCEPTED    -> DISPATCHED，保留 ID
    +-- SERVER_BUSY -> 移除本次记录，返回 SERVER_BUSY
    +-- 其他回复 / 网络异常 -> DISPATCH_UNKNOWN，保留 ID
```

[查看分发流程图片](docs/diagrams/flow-3.png)

[Mermaid source](docs/diagrams/flow-3.mmd)

`handleSubmit` 将提交结果编码为 `结果|taskId` 返回 Client。协议解析错误则返回 `ERROR|错误类型`。

当前从 `ConcurrentHashMap` 遍历结果中选择第一个 `ALIVE` Worker，没有轮询、负载比较或失败后换一个 Worker 的逻辑。`ALIVE` 仅代表心跳状态，不保证端口此刻可连接或还有执行容量。

`dispatchTask` 在 Scheduler 的连接处理线程中同步等待确认，但不会等待任务执行完成。连接异常也统一按 `DISPATCH_UNKNOWN` 保守处理，即使其中某些异常发生在任务发送之前。

## 4. Worker 接收与并发执行

```text
accept -> connectionPool（2 个线程，20 个等待位置）
    +-- 连接处理被拒绝 -> 关闭 Socket
    |
    v
handleTask：读取并校验 EXECUTE
    +-- 参数非法 -> ERROR
    |
    v
taskPool.execute（3 个线程，10 个等待位置）
    +-- 容量耗尽 -> SERVER_BUSY
    |
    +-- 接受 -> 连接线程回复 ACCEPTED
           |
           +-> 执行线程异步执行，需要时先排队
                    |
                 打印 starts
                    |
                 Thread.sleep
                    +-- 正常结束 -> 打印 completed
                    +-- 被中断 -> 恢复中断标志，打印 interrupted

回复 ACCEPTED 与开始执行之间没有固定的先后顺序。
```

[查看并发执行图片](docs/diagrams/flow-4.png)

[Mermaid source](docs/diagrams/flow-4.mmd)

提交成功后，回复确认和执行任务发生在不同线程，两者没有固定先后顺序；短任务甚至可能在确认到达 Scheduler 之前就完成。Worker 当前没有任务去重表，也不向 Scheduler 回报执行结果。

## 5. 线程池与队列

| 所在进程 | 线程池 | 核心 / 最大线程数 | 等待队列 | 职责 |
|---|---|---|---|---|
| Scheduler | `connectionPool` | 2 / 4 | 20 | 处理三类请求；提交请求还会等待下发确认 |
| Scheduler | `failureDetector` | 单线程 | 定时调度 | 每 1 秒检查 Worker 心跳 |
| Worker | `connectionPool` | 2 / 2 | 20 | 解析 EXECUTE 并回复接收结果 |
| Worker | `taskPool` | 3 / 3 | 10 | 并发执行任务 |
| Worker | `heartbeatExecutor` | 单线程 | 定时调度 | 每 2 秒尝试发送心跳，首次立即运行 |

Java `ThreadPoolExecutor` 通常先创建核心线程，再将任务放入队列；队列满后才继续创建线程，直到最大线程数。线程数和队列容量都耗尽后，本项目的 `AbortPolicy` 会抛出 `RejectedExecutionException`。

因此，Worker 的执行线程池最多容纳 3 个执行中的任务和 10 个等待任务。连接队列保存的是网络处理工作，与执行队列不同。两端连接池拒绝连接处理任务时直接关闭 Socket；只有 Worker 的执行池拒绝任务时，才明确回复 `SERVER_BUSY`。

`putIfAbsent` 把“判断 ID 是否存在”和“写入记录”合为一个原子操作，防止多个提交线程同时接受同一个任务 ID。线程安全的数据结构并不意味着整个网络分发流程是一个原子事务。

## 6. 心跳和故障检测

```text
Worker 心跳线程：每 2 秒尝试发送 HEARTBEAT
    |
Scheduler 查找 Worker ID
    +-- 未注册 -> ERROR|WORKER_NOT_REGISTERED
    +-- 已注册 -> 更新 lastHeartbeat
                     |
                 若为 SUSPECTED_DEAD，恢复 ALIVE
                     |
                 返回 HEARTBEAT_ACK

Scheduler 检查线程：每 1 秒检查
    |
距上次心跳 > 5 秒，且当前 ALIVE？
    +-- 是 -> SUSPECTED_DEAD，后续分发不再选择它
    +-- 否 -> 保持状态
```

[查看心跳检测图片](docs/diagrams/flow-5.png)

[Mermaid source](docs/diagrams/flow-5.mmd)

`SUSPECTED_DEAD` 表示“怀疑失联”，不等于确定进程已经死亡；网络延迟也可能导致心跳缺失。当前故障检测只修改 Worker 状态，不处理已分发的任务。

心跳与任务执行隔离，所以耗时任务不会直接占用心跳线程。不过注册和心跳连接当前没有显式设置连接、读取超时，心跳请求可能因网络阻塞而推迟；每 2 秒是调度周期，不是严格的到达保证。心跳失败只打印日志，没有自动重新注册。

## 7. 当前任务状态

```text
创建 Task -> PENDING
                +-- 明确接收确认 -> DISPATCHED
                +-- 未获得可靠确认 -> DISPATCH_UNKNOWN

当前没有执行结果回报，不会自动进入 RUNNING / SUCCESS / FAILED。
```

[查看状态转换图片](docs/diagrams/flow-6.png)

[Mermaid source](docs/diagrams/flow-6.mmd)

没有可用 Worker 或明确收到 `SERVER_BUSY` 时，本次记录会从 `tasks` 中移除，允许之后使用相同 ID 再提交。非法任务不进入记录表；重复提交不覆盖原记录。

`DISPATCHED` 和 `DISPATCH_UNKNOWN` 当前都保留 ID，再次提交会返回 `DUPLICATE`。枚举中虽然存在 `RUNNING`、`SUCCESS`、`FAILED`，但当前远程执行流程没有更新这些状态。`getTaskStatus()` 只是 Java 方法，尚未开放 TCP 查询命令。

## 8. 协议和当前边界

| 方向 | 请求示例 | 正常响应 |
|---|---|---|
| Client → Scheduler | `SUBMIT\|task-001\|5000` | `ACCEPTED\|task-001` |
| Worker → Scheduler | `REGISTER\|worker-1\|localhost\|9001` | `REGISTERED\|worker-1` |
| Worker → Scheduler | `HEARTBEAT\|worker-1` | `HEARTBEAT_ACK\|worker-1` |
| Scheduler → Worker | `EXECUTE\|task-001\|5000` | `ACCEPTED\|task-001` |

协议以换行结束，每条请求使用一个短连接。示例 Client 只提交一次固定 ID 的任务，打印接收结果后退出。

当前已实现：注册、心跳、失联标记与恢复、Scheduler 内存去重、远程下发、Worker 并发执行和有界队列拒绝。

当前未实现：结果回报、远程状态查询、自动重试、故障任务重新分配、Worker 端幂等、持久化和负载均衡。内存记录随进程退出丢失，因此当前不提供跨重启的去重或任务恢复保证。

## 9. 代码入口与验证

- [Scheduler.java](src/main/java/scheduler/Scheduler.java)：请求分流、`submitTask`、`dispatchTask`、注册与心跳检测。
- [Worker.java](src/main/java/worker/Worker.java)：`start`、`handleTask`、`executeTask`、心跳发送。
- [Client.java](src/main/java/client/Client.java)：示例提交入口。
- [TaskStatus.java](src/main/java/scheduler/TaskStatus.java)：任务状态枚举。
- [verify_dispatch.py](scripts/verify_dispatch.py)：双 JVM 集成验证，覆盖并发、队列拒绝、心跳、去重及分发未知结果。

在项目根目录执行以下命令进行验证，运行前需空出 8080 和 9001 端口：

```powershell
mvn -q test
python scripts/verify_dispatch.py
```
